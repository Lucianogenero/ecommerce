<%= form_for(subcategoriaproduto) do |f| %>
  <% if subcategoriaproduto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subcategoriaproduto.errors.count, "error") %> prohibited this subcategoriaproduto from being saved:</h2>

      <ul>
      <% subcategoriaproduto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :categoriaproduto_id %>
    <%= f.select("categoriaproduto_id", Categoriaproduto.all.collect {|p| [ p.nome, p.id ] }, include_blank: true, selected: @subcategoriaproduto.categoriaproduto_id ) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
