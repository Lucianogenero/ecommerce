<%= form_for(estoque) do |f| %>
  <% if estoque.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estoque.errors.count, "error") %> prohibited this estoque from being saved:</h2>

      <ul>
      <% estoque.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :preco %>
    <%= f.text_field :preco %>
  </div>

  <div class="field">
    <%= f.label :saldo %>
    <%= f.text_field :saldo %>
  </div>

  <div class="field">
    <%= f.label :produto_id %>
    <%= f.select("produto_id", Produto.all.collect {|p| [ p.nome, p.id ] }, include_blank: true, selected: @estoque.produto_id ) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
